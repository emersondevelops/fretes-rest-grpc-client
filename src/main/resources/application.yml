micronaut:
  application:
    name: fretesRest

grpc:
  client:
    plaitext: true
    max-retry-attempts: 10
  channels:
    fretes:
      address: 'localhost:50051'
      plaintext: true
      max-retry-attempts: 10